export const sendOTP = async (phone, otp) => {
  console.log("=================================");
  console.log(`Sending OTP to ${phone}`);
  console.log(`OTP: ${otp}`);
  console.log("=================================");
};